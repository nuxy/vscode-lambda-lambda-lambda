{
  "name": "{{appName}}",
  "version": "0.0.1",
  "description": "{{appDescription}}",
  "main": "app.js",
  "dependencies": {
    "lambda-lambda-lambda": "latest"
  },
  "devDependencies": {
    "eslint": "latest",
    "jsdoc": "latest"
  },
  "scripts": {
    "gendoc": "jsdoc -d doc -r src",
    "lint": "eslint src/*.js"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
