{
  "name": "{{appName}}",
  "version": "0.0.1",
  "description": "{{appDescription}}",
  "main": "app.js",
  "dependencies": {
    "lambda-lambda-lambda": "latest"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
