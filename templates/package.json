{
  "name": "{{appName}}",
  "version": "0.0.1",
  "description": "{{appDescription}}",
  "main": "app.js",
  "dependencies": {
    "lambda-lambda-lambda": "^{{lambdaDepVersion}}"
  },
  "engines": {
    "node": ">=12.0.0"
  }
}
